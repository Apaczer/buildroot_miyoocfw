diff --git a/libbb/printable_string.c b/libbb/printable_string.c
index a814fd03c..a3ac9493b 100644
--- a/libbb/printable_string.c
+++ b/libbb/printable_string.c
@@ -28,8 +28,10 @@ const char* FAST_FUNC printable_string2(uni_stat_t *stats, const char *str)
 		}
 		if (c < ' ')
 			break;
+		/*
 		if (c >= 0x7f)
 			break;
+		*/
 		s++;
 	}
 
@@ -42,7 +44,7 @@ const char* FAST_FUNC printable_string2(uni_stat_t *stats, const char *str)
 			unsigned char c = *d;
 			if (c == '\0')
 				break;
-			if (c < ' ' || c >= 0x7f)
+			if (c < ' '/* || c >= 0x7f */)
 				*d = '?';
 			d++;
 		}
diff --git a/libbb/unicode.c b/libbb/unicode.c
index e98cbbf35..6de3e91eb 100644
--- a/libbb/unicode.c
+++ b/libbb/unicode.c
@@ -1027,7 +1027,7 @@ static char* FAST_FUNC unicode_conv_to_printable2(uni_stat_t *stats, const char
 					while ((int)--width >= 0);
 					break;
 				}
-				*d++ = (c >= ' ' && c < 0x7f) ? c : '?';
+				*d++ = (c >= ' ' /* && c < 0x7f */) ? c : '?';
 				src++;
 			}
 			*d = '\0';
@@ -1035,7 +1035,7 @@ static char* FAST_FUNC unicode_conv_to_printable2(uni_stat_t *stats, const char
 			d = dst = xstrndup(src, width);
 			while (*d) {
 				unsigned char c = *d;
-				if (c < ' ' || c >= 0x7f)
+				if (c < ' '/* || c >= 0x7f */)
 					*d = '?';
 				d++;
 			}
